# Core
flask>=2.3.0
requests==2.31.0
python-dotenv>=1.0.0
psutil>=5.9.0

# LLM / AI
torch>=2.1.0
# (sentence-transformers removed — vector memory now uses local TF-IDF, no neural embeddings)

# Local neural TTS (Kokoro-82M)
kokoro>=0.9.4
soundfile>=0.12.1

# Document reading (PDF/DOCX/PPTX/XLSX/OCR/audio → text)
markitdown>=0.1.6

# Web search (DuckDuckGo, no API key)
ddgs>=9.0.0

# STT
faster-whisper>=1.0.0

# TTS
pygame==2.6.1
edge-tts==6.1.12

# Browser
playwright>=1.40.0

# News / RSS
feedparser>=6.0.11

# Web scraping
beautifulsoup4==4.12.3
lxml==5.2.1

# Audio (CLI voice mode only)
sounddevice==0.4.7
numpy==1.26.4
SpeechRecognition==3.10.1
pyaudio==0.2.14
